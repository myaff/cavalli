.product {
	&__img {
    margin-bottom: vh(90);

    img {
      width: 100%;
    }
  }

  &__name {
    font: 30px/1.33 $font-base;
    margin-bottom: 1.5rem;
  }

  &__price {
    font: bold 40px/1 $font-util;
  }
}